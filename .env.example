# App (Vite) — obrigatório para `npm run dev` / build
VITE_SUPABASE_URL=https://hziovsgaqmrwthnlqobd.supabase.co
VITE_SUPABASE_ANON_KEY=

# Opcional: scripts na máquina local (nunca expor no browser)
# Para encerrar serviço pela linha de comando: npm run script:close-service -- <uuid>
SUPABASE_URL=https://hziovsgaqmrwthnlqobd.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# Migrações: `npm run db:push` grava no histórico do Supabase (requer CLI linkado + login).
# Se o remoto tiver migrações que não existem no repo, aplique um .sql sem tocar no histórico:
#   npm run db:sql:linked -- supabase/migrations/20260423140000_log_billing_km_constraints.sql
# (usa o projeto ligado em supabase/.temp; autenticação via `npx supabase login`.)
